---
// src/components/GalleryGrid.astro

const projects = [
  {
    id: 1,
    title: "The Mira Apartments",
    location: "Apopka, FL",
    image: "/slider/slider_0.png",
    units: "320",
    area: "459,850 GSF",
    scope: "Rough Framing",
    contractor: "LandSouth Construction",
    description:
      "Causeway Apartments is a completed multifamily development in Pasco County, Florida, consisting of multiple four-story residential buildings and associated amenity structures constructed primarily with a wood-framed structural system over slab-on-grade foundations.\n\nThe work included conventional and engineered wood framing for exterior and interior load-bearing walls, floor systems, roof truss systems, stair towers, elevator shafts, corridors, and breezeways, coordinated with fire-rated and shear wall assemblies per Florida Building Code and high-wind design requirements.",
  },
  {
    id: 2,
    title: "Poinciana Parc Apartments",
    location: "Poinciana, FL",
    image: "/slider/slider_1.png",
    units: "280",
    area: "350,000 GSF",
    scope: "Rough Framing & Exterior",
    contractor: "Coastal Construction",
    description:
      "Poinciana Parc features high-density residential living with a focus on sustainable framing techniques and efficient floor layouts.",
  },
  {
    id: 3,
    title: "Proxi at Lake Mary",
    location: "Lake Mary, FL",
    image: "/slider/slider_2.png",
  },
  {
    id: 4,
    title: "Gables at Baldwin Park",
    location: "Orlando, FL",
    image: "/slider/slider_3.png",
  },
  {
    id: 5,
    title: "V3 Apartments",
    location: "Fort Myers, FL",
    image: "/slider/slider_4.png",
    size: "xl",
  },
  {
    id: 6,
    title: "Cypress Junction Montessori School",
    location: "Winter Haven, FL",
    image: "/slider/slider_5.png",
  },
  {
    id: 7,
    title: "Warner University - New Residence Hall",
    location: "Lake Wales, FL",
    image: "/slider/slider_0.png",
  },
  {
    id: 8,
    title: "Lake Wire Mixed",
    location: "Lakeland, FL",
    image: "/slider/slider_1.png",
  },
  {
    id: 9,
    title: "Brentwood Multifamily",
    location: "Davenport, FL",
    image: "/slider/slider_2.png",
    size: "xl",
  },
  {
    id: 10,
    title: "Haven 41 Causeway Apartments",
    location: "Pasco County, FL",
    image: "/slider/slider_3.png",
  },
];
---

<section class="gallery-grid">
  <div class="grid-container">
    <!-- Row 1 -->
    <div class="row row-3" data-reveal>
      {
        projects.slice(0, 3).map((p) => (
          <div class="banner project-trigger" data-project={JSON.stringify(p)}>
            <div class="overlay" />
            <div class="content">
              <h4>{p.title}</h4>
              <p>{p.location}</p>
            </div>
          </div>
        ))
      }
    </div>

    <!-- Row 2 -->
    <div class="row row-mix-1-2" data-reveal>
      {
        projects.slice(3, 5).map((p) => (
          <div
            class={`banner project-trigger ${
              p.size === "xl" ? "banner-xl" : ""
            }`}
            data-project={JSON.stringify(p)}
          >
            <div class="overlay" />
            <div class="content">
              <h4>{p.title}</h4>
              <p>{p.location}</p>
            </div>
          </div>
        ))
      }
    </div>

    <!-- Row 3 -->
    <div class="row row-3" data-reveal>
      {
        projects.slice(5, 8).map((p) => (
          <div class="banner project-trigger" data-project={JSON.stringify(p)}>
            <div class="overlay" />
            <div class="content">
              <h4>{p.title}</h4>
              <p>{p.location}</p>
            </div>
          </div>
        ))
      }
    </div>

    <!-- Row 4 -->
    <div class="row row-mix-2-1" data-reveal>
      {
        projects.slice(8, 10).map((p) => (
          <div
            class={`banner project-trigger ${
              p.size === "xl" ? "banner-xl" : ""
            }`}
            data-project={JSON.stringify(p)}
          >
            <div class="overlay" />
            <div class="content">
              <h4>{p.title}</h4>
              <p>{p.location}</p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
  ...
  <script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
      const triggers = document.querySelectorAll(".project-trigger");
      triggers.forEach((t) => {
        t.addEventListener("click", () => {
          const projectData = JSON.parse(t.getAttribute("data-project"));
          if (window.openProjectModal) {
            window.openProjectModal(projectData);
          }
        });
      });
    });
  </script>
</section>



<style>
  .gallery-grid {
    width: 100%;
    background: #121212;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    transform: scale(0.96);
    transform-origin: center top;
    will-change: transform;
  }

  .grid-container {
    display: flex;
    flex-direction: column;
    gap: 6px;
    width: 100%;
    max-width: 1536px;
  }

  .row {
    display: flex;
    gap: 6px;
    height: 324px;
    width: 100%;
  }

  .banner {
    position: relative;
    height: 100%;
    background: #1e1e1e; /* Fallback color */
    background-size: cover;
    background-position: center;
    flex: 1;
    overflow: hidden;
  }

  /* Specific Banner Backgrounds (Mock data from /slider) */
  .row-3:nth-child(1) .banner:nth-child(1) {
    background-image: url("/slider/slider_0.png");
  }
  .row-3:nth-child(1) .banner:nth-child(2) {
    background-image: url("/slider/slider_1.png");
  }
  .row-3:nth-child(1) .banner:nth-child(3) {
    background-image: url("/slider/slider_2.png");
  }

  .row-mix-1-2 .banner:nth-child(1) {
    background-image: url("/slider/slider_3.png");
  }
  .row-mix-1-2 .banner-xl {
    background-image: url("/slider/slider_4.png");
  }

  .row-3:nth-child(3) .banner:nth-child(1) {
    background-image: url("/slider/slider_5.png");
  }
  .row-3:nth-child(3) .banner:nth-child(2) {
    background-image: url("/slider/slider_0.png");
  }
  .row-3:nth-child(3) .banner:nth-child(3) {
    background-image: url("/slider/slider_1.png");
  }

  .row-mix-2-1 .banner-xl {
    background-image: url("/slider/slider_2.png");
  }
  .row-mix-2-1 .banner:nth-child(2) {
    background-image: url("/slider/slider_3.png");
  }

  .overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(0deg, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6));
    transition: opacity 0.3s ease;
  }

  .banner:hover .overlay {
    background: linear-gradient(0deg, rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4));
  }

  .banner-xl {
    flex: 2;
  }

  /* Pixel perfect widths from Figma for 1536px width */
  .row-mix-1-2 .banner:not(.banner-xl) {
    flex: 0 0 508px;
  }
  .row-mix-1-2 .banner-xl {
    flex: 0 0 1022px;
  }

  .row-mix-2-1 .banner-xl {
    flex: 0 0 1022px;
  }
  .row-mix-2-1 .banner:not(.banner-xl) {
    flex: 0 0 508px;
  }

  .content {
    position: absolute;
    bottom: 25px;
    left: 32px;
    width: calc(100% - 64px);
    max-width: 444px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    z-index: 2;
    pointer-events: none;
  }

  .content-xl {
    max-width: 958px;
  }

  h4 {
    font-family: "Roboto", sans-serif;
    font-weight: 600;
    font-size: 24px;
    line-height: 1.4;
    letter-spacing: -0.02em;
    color: #fcfcfc;
    margin: 0;
  }

  p {
    font-family: "Roboto", sans-serif;
    font-weight: 400;
    font-size: 18px;
    line-height: 1.7;
    letter-spacing: -0.02em;
    color: #ffffff;
    margin: 0;
  }

  /* Pagination */
  .pagination {
    margin: 60px 0 100px;
    display: flex;
    gap: 6px;
    justify-content: center;
    align-items: center;
  }

  .page-control,
  .page-num {
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #ffffff;
    border: 1px solid #e9e9e9;
    font-family: "Roboto", sans-serif;
    font-size: 12px;
    cursor: pointer;
    transition: all 0.3s ease;
    user-select: none;
    outline: none;
    padding: 0;
  }

  .page-control {
    padding: 8px 12px;
    gap: 4px;
    color: #1b1b1b;
  }

  .page-num {
    width: 35px;
    color: #313131;
  }

  .page-num.active {
    background: #e6a523;
    border-color: #e6a523;
    color: #121212;
    font-weight: 600;
  }

  .page-num:hover:not(.active),
  .page-control:hover {
    background: #f5f5f5;
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .grid-container {
      padding: 0 20px;
    }
    .row-mix-1-2 .banner,
    .row-mix-2-1 .banner {
      flex: 1;
    }
  }

  @media (max-width: 768px) {
    .row {
      flex-direction: column;
      height: auto;
    }
    .banner {
      height: 324px;
      flex: none !important;
      width: 100% !important;
    }
    .content {
      max-width: 100%;
    }
    .pagination {
      flex-wrap: wrap;
      padding: 0 20px;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const galleryGridSection = document.querySelector('.gallery-grid') as HTMLElement;

    if (galleryGridSection) {
      let ticking = false;

      function updateAnimation() {
        const rect = galleryGridSection.getBoundingClientRect();
        const windowHeight = window.innerHeight;
        
        let progress = 1 - (rect.top / windowHeight);
        progress = Math.max(0, Math.min(progress, 1));
        
        if (progress >= 1) {
          galleryGridSection.style.transform = 'none';
        } else {
          const scale = 0.96 + (progress * 0.04);
          galleryGridSection.style.transform = `scale(${scale})`;
        }
        ticking = false;
      }

      window.addEventListener('scroll', () => {
        if (!ticking) {
          window.requestAnimationFrame(updateAnimation);
          ticking = true;
        }
      }, { passive: true });

      updateAnimation();
    }
  });
</script>
